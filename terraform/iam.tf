# IAM Note:
# The cloudboxd-terraform user has S3FullAccess.
# In production, a dedicated pipeline IAM role with least-privilege
# S3 policy would be provisioned here.
# Skipped in dev: IAMFullAccess not granted to terraform user.
